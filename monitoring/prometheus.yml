global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: stackpilot-api
    metrics_path: /health
    static_configs:
      - targets: ["api:8000"]
