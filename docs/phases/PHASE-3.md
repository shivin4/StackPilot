# Phase 3 — Cloud & Advanced

## Features

1. **Azure Blob** — archive deployment logs
2. **HTTPS** — Let's Encrypt on EC2
3. **Prometheus + Grafana** — metrics dashboard
4. **GCP Cloud Run** — deploy sample app (portfolio screenshot)
5. **Kubernetes** — sample app on Minikube via `k8s/`

## Tasks

- [ ] Implement `backend/app/services/log_storage.py` (Azure SDK)
- [ ] Certbot on EC2 for HTTPS
- [ ] Import Grafana dashboard
- [ ] `gcloud run deploy` for `samples/python-hello`
- [ ] `kubectl apply -f k8s/sample-app/`

## Success criteria

Resume can honestly list: AWS EC2, Azure Blob, GCP Cloud Run, Docker, K8s, Prometheus, Grafana, GitHub Actions.
